# Public synthetic demo

The runnable demo fixtures live in [tests/fixtures](../../tests/fixtures) and the readable review artifact is [public-demo-review.md](../../tests/outputs/public-demo-review.md). All identifiers are `SYN-*`; no real candidate, project, employer, vacancy or provenance is represented.
